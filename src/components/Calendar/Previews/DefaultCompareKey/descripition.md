Đây là trường hợp muốn chuyền một compareKey mặc định, sử dụng bằng cách khai báo: 
```jsx
<Calendar 
    compareKey='previous_period' 
/>
```
Nếu `compareKey='custom'` thì phải truyền thêm fromRange, toRange